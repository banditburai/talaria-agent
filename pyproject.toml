[project]
name = "talaria"
version = "0.1.0"
description = "Talaria - A modular AI agent system (development workspace)"
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
    "ruff>=0.1.0",
    "ty>=0.0.21",
]
